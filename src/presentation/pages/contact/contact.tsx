import React from "react";
import transition from "@/presentation/components/transitions/transition";

const Contact: React.FC = () => {
    return <h1>Contact</h1>;
};

export default transition(Contact);
